<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Untuk daftar aplikasi + label/ikon. Tanpa ini hanya app kita sendiri yang terlihat. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- v1.4 — Perisai anti ghost-touch memasang jendela overlay (dikelola app ini sendiri
         lewat AppOps). Tetap TANPA izin internet seperti sebelumnya. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".AppsPermsApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:localeConfig="@xml/locales_config"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.OverlayOps">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Simpan pilihan bahasa otomatis di Android < 13 (dipakai AppCompat). -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <!-- Wajib untuk Shizuku: mengizinkan Shizuku mengirim binder user service ke app ini -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!-- Perisai anti ghost-touch: foreground service yang menempelkan pita
             penangkap sentuhan di tepi layar. specialUse = tidak ada tipe FGS yang
             lebih cocok untuk "window guard" di API 34. -->
        <service
            android:name=".guard.GhostGuardService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Ghost-touch guard bands drawn as touch-consuming overlays" />
        </service>

        <!-- Hanya menerima PendingIntent aksi dari notifikasi app ini sendiri -->
        <receiver
            android:name=".guard.GhostGuardReceiver"
            android:exported="false" />

    </application>

</manifest>
